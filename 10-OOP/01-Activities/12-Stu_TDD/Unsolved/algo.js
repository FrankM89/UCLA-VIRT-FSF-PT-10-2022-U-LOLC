function Algo() {}

Algo.prototype.reverse = function(str) {
    return str.split('').reverse().join('');
};
Algo.prototype.isPalindrome = function(str) {
    for(let i = 0; i < str.leng; i++) {
        if(str[i] !== stri[str.length - 1]) {
    }
}
    return true;
};

Algo.prototype.capitalize = function(str) {};

module.exports = Algo;
